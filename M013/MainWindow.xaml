<Window x:Class="M013.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:M013"
        Title="MainWindow" Height="450" Width="800"
		WindowStartupLocation="CenterScreen" Topmost="True"
		DataContext="{Binding RelativeSource={RelativeSource Self}}">
	<StackPanel>
		<!--Commands-->
		<!--Dedizierte Klassen für die Logik von Events in der GUI-->
		<!--Warum? Um die Logik sauber von den EventHandlern zu trennen (MVVM)-->
		<CheckBox Content="Button aktiviert" Name="CB"/>
		<Button Command="{Binding Exit}" Content="Hallo"
				CommandParameter="{Binding ElementName=CB, Path=IsChecked}"/>

		<Button Command="{Binding CustomExit}" Content="Hallo"
				CommandParameter="{Binding ElementName=CB, Path=IsChecked}"/>

		<TextBlock Name="Output"/>
		<TextBox Name="TB">
			<b:Interaction.Triggers>
				<b:EventTrigger EventName="KeyDown">
					<b:InvokeCommandAction Command="{Binding EnterCommand}"
										   PassEventArgsToCommand="True"/>
				</b:EventTrigger>
			</b:Interaction.Triggers>
		</TextBox>
	</StackPanel>
</Window>